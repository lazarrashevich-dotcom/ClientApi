meta:
  environment: csharp
  toolchain:
    name: dotnet
    version: "8.0"
build:
  dockerfile: false
run:
  image: mcr.microsoft.com/dotnet/sdk:8.0
  buildFileName: bin/properties
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
